# MindQuark 黑客松提交清单

## 当前代码状态

项目已完成本地安全加固、前后端聊天历史契约统一、危机内容短路、语音资源释放、测试恢复、类型检查、仓库卫生整理与提交文档补全。根目录中的依赖目录和 `dist/` 已从 Git 索引移除，但本地文件仍保留，因此不会影响当前开发和运行。

| 验证项 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm test` | 5 个测试文件、28 个测试全部通过 |
| `npm run build` | 通过 |
| `node --check functions/api/index.js` | 通过 |
| `npm audit --omit=dev` | 0 个已知生产依赖漏洞 |
| 本地 API 健康检查 | 通过 |
| 非法 CORS 来源 | 返回 403 |
| 危机文本 API 路径 | 返回 `isCrisis: true` 且不触发模型调用 |

## 提交前必须手动完成的部署操作

> 下列操作需要服务商控制台权限，因此未在本地代码修复中自动执行。

| 优先级 | 操作 | 完成标准 |
|---|---|---|
| P0 | 立即撤销并重新生成所有曾出现在旧源码或提交记录中的模型、语音服务密钥。 | 旧密钥失效；新密钥只存在于 CloudBase 环境变量或本地未跟踪 `.env` 文件。 |
| P0 | 在 CloudBase 的 `api` 函数环境变量中配置 `OPENROUTER_API_KEY`、所需备份模型凭据、`XF_APPID`、`XF_API_SECRET` 与 `XF_API_KEY`。 | `/api/chat`、`/api/iat-auth`、`/api/tts` 使用环境变量成功工作，源码中不存在凭据。 |
| P0 | 配置 `CORS_ORIGINS` 为实际生产演示网址与需要支持的本地地址。 | 生产网页能访问 API；未知 Origin 被拒绝；绝不使用 `*`。 |
| P1 | 部署更新后的 `api` 函数与客户端构建。 | `GET /api/health` 返回 `status: ok`；线上危机文本返回静态支持资源。 |
| P1 | 用新的干净克隆执行 `npm install`、`npm run typecheck`、`npm test` 与 `npm run build`。 | 评委机器可复现所有验证结果。 |

## 现场演示建议

先展示首页与两种陪伴人格，再使用一个非敏感的普通情绪提示演示聊天连续性。随后展示一次呼吸或感官练习，并展示个人头像/人格设置。若展示危机安全路径，请使用预先准备、非个人化的示例文本，并说明系统在命中高风险内容时会停止模型生成，转而返回即时人类支持资源。

## 已知的非阻塞优化项

当前生产 JavaScript 包约为 654 kB（gzip 后约 210 kB），Vite 会给出超过 500 kB 的提示。它不影响当前构建和功能，但后续可以将聊天、呼吸和设置页改为按路由动态加载，以进一步改善首次加载速度。

## Git 提交建议

提交前检查 `git status`，确认不包含 `.env`、真实凭据、`node_modules/` 或 `dist/`。本次仅修改工作区与 Git 索引，未创建提交、未推送远程仓库，也未部署云端服务。
